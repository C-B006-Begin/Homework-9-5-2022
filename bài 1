#include <stdio.h>
int main()
{
    int count = 0;
    char str[]="hello world";
    while(str[count]!='\0'){
        count++;
    }
    for (int i = count; i >= 0; i--){
        printf("%c", str[i]);
    }
}
